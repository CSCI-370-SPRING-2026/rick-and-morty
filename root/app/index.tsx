import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const index = () => {
  return (
    // check if user is logged in -> render tabs if so -> if not logged in -> render login screen
    <></>
  )
}

export default index

const styles = StyleSheet.create({})